# Use the file name mbox-short.txt as the file name
fname = input("Enter file name: ")
fh = open(fname)
x=0
n=0
for line in fh:
	if not line.startswith("X-DSPAM-Confidence:") : 
		continue
	elif line.startswith("X-DSPAM-Confidence"):
		e=line.find(" ")
        p=line[e:]
        q=p.strip()
        g=float(q)
        x=x+g
        n=n+1
print("Average spam confidence:",(x/n))
